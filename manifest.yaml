---
applications:
  - name: monitoring
    routes:
      - route: identity-give-monitoring-((ENVIRONMENT_NAME)).apps.internal
    memory: ((MEMORY))
    instances: ((INSTANCES))
    buildpacks:
      - binary_buildpack
